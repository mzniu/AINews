# 复制为 forbidden_words.local.yaml 后生效（该文件已 gitignore，不会提交）
# 在默认词表基础上追加词条；同 id 的 category 可设 enabled: false 关闭整类

version: 1

categories:
  - id: custom_local
    name: 本地自定义禁词
    enabled: true
    severity: error
    match: substring
    words:
      - 示例禁词
